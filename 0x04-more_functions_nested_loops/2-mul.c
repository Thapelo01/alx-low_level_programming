#include "main.h"
/**
 * mul - Multiplies two #
 *@x: The first # to be multiply
 *@y: The first # to be multiply with first #
 * Return: The result of multiplication between x param and y param
 */
int mul(int x, int y)
{
	return (x * y);
}
