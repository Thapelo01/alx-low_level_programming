#include "main.h"
/**
 * _isupper - Checks if a char is a digit
 *@x: The # to be checked
 * Return: 1 for a char that will be a digit or 0 for anything
 */
int _isdigit(int c)
{
	if (x >= 48 && x <= 57)
	{
	return (1);
	}
	return (0);
}
